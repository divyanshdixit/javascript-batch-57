// assignment (=)

var a = 20;

// comparison op: (==, ===)

10 == "10" // true (value check only)
10 === "10" // (value and datatype) false

// airthmatic (+,-,*,/,**,%)
 // + (addition, concat)
10 % 10

// logical (!(not), ||, &&)

!false  => true

(10 > 11 || 11 > 12) => false || false => false else true

(11 > 10 && 12 > 11) => true and true => true else false


// type operator(typeof, instanceof)

var x=0

typeof x; // number

// terniary op: ( ? :) short form of if else

if(cond){

}else{

}

(true) ? 'true' : 'false' 